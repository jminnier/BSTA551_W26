{
  "hash": "d53b3957c0342e7a110209536ea610e4",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Lesson 9: Introduction to Bootstrap for Statistical Inference\"\nsubtitle: \"BSTA 551: Statistical Inference\"\nauthor: \"Jessica Minnier\"\ntitle-slide-attributes:\n    data-background-color: \"#006a4e\"\ndate: \"2026-01-28\"\nformat: \n  revealjs:\n    theme: \"../simple_NW.scss\"\n    chalkboard: true\n    scrollable: true\n    slide-number: true\n    show-slide-number: all\n    width: 1955\n    height: 1100\n    footer: Lesson 9 - Bootstrap Introduction\n    html-math-method: mathjax\n    highlight-style: atom-one\nexecute:\n  echo: true\n  warning: false\n  message: false\n---\n\n\n\n# Lesson 9: The Bootstrap {background-color=\"#2c3e50\"}\n\n## Review: Where We Left Off\n\n**Lesson 8: One-Sample t-Intervals and Population Proportions**\n\n-   t-intervals when $\\sigma$ is unknown: $\\bar{x} \\pm t_{\\alpha/2, n-1}\\frac{s}{\\sqrt{n}}$\n-   Normality assumption for t-intervals\n-   Confidence intervals for proportions (Wald, Score, Agresti-Coull)\n\n. . .\n\n**The Problem:** What if normality doesn't hold and the sample size is small?\n\n. . .\n\n**Today's Goals:**\n\n1. Understand the bootstrap principle and its theoretical foundation\n2. Learn why resampling **with replacement** is essential\n3. Explore asymptotic properties of bootstrap estimates\n4. Understand when bootstrap methods fail\n5. Construct bootstrap percentile and t confidence intervals\n\n## Motivating Example: Drug Response Times\n\nIn a clinical trial, 12 patients' response times (days) to a new medication:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nresponse_times <- c(8, 12, 15, 7, 22, 45, 11, 9, 14, 18, 10, 13)\n\ntibble(x = response_times) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 8, fill = \"steelblue\", color = \"white\", alpha = 0.7) +\n  geom_vline(xintercept = mean(response_times), color = \"red\", linewidth = 1.2, linetype = \"dashed\") +\n  labs(x = \"Response Time (days)\", y = \"Count\",\n       title = \"Distribution of Drug Response Times\") +\n  annotate(\"text\", x = mean(response_times) + 5, y = 3, \n           label = paste0(\"Mean = \", round(mean(response_times), 1)), color = \"red\", size = 5)\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-1-1.png){width=672}\n:::\n:::\n\n\n## The Problem with Small Samples\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Q-Q plot to check normality\nqqnorm(response_times, main = \"Normal Q-Q Plot\", pch = 19, col = \"steelblue\")\nqqline(response_times, col = \"red\", lwd = 2)\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-2-1.png){width=672}\n:::\n:::\n\n\n. . .\n\n**Issues:**\n\n- Small sample (n = 12) \n- Evidence of right skew (outlier at 45 days)\n- Normal-based t-interval may not have correct coverage\n\n## The Key Question\n\nHow can we construct confidence intervals when:\n\n1. The population distribution is **unknown or non-normal**?\n2. The sample size is **too small** for CLT to apply reliably?\n3. We want to estimate parameters **other than the mean** (median, trimmed mean, ratio)?\n\n. . .\n\n::: callout-tip\n## The Bootstrap Solution\n**Use the data itself** to approximate the sampling distribution of our statistic!\n:::\n\n# The Bootstrap Idea {background-color=\"#3498db\"}\n\n## What is the Bootstrap?\n\n::: callout-important\n## Definition (Devore, Berk, Carlton 8.5)\nThe **bootstrap** is a resampling method that uses the observed sample to approximate the sampling distribution of a statistic.\n\nThe method was developed by **Bradley Efron** in the late 1970s and substitutes heavy computation for theoretical derivation.\n:::\n\n. . .\n\n**Key insight:** The sample is our best estimate of the population. If we resample *from the sample*, we can learn about sampling variability.\n\n## Traditional vs. Bootstrap Approach\n\n**Traditional Inference:**\n\n1. Assume population distribution (e.g., Normal)\n2. Derive sampling distribution theoretically\n3. Use theoretical distribution to construct intervals\n\n. . .\n\n**Bootstrap Inference:**\n\n1. Use the sample as a proxy for the population\n2. Generate sampling distribution empirically via resampling\n3. Use empirical distribution for inference\n\n## The Plug-In Principle (Chihara & Hesterberg 5.2)\n\n::: callout-important\n## The Plug-In Principle\nTo estimate a parameter (a quantity that describes the **population**), use the statistic that is the corresponding quantity for the **sample**.\n:::\n\n. . .\n\n**Examples:**\n\n| Population Parameter | Sample Statistic |\n|---------------------|------------------|\n| Mean $\\mu$ | $\\bar{x}$ |\n| Standard deviation $\\sigma$ | $s$ |\n| Median $\\tilde{\\mu}$ | Sample median |\n| Standard error $\\sigma/\\sqrt{n}$ | $s/\\sqrt{n}$ |\n\n. . .\n\n**The bootstrap extends this:** Use the **entire sample** as an estimate of the **entire population**.\n\n## The Empirical Distribution Function\n\n::: callout-important\n## Definition: Empirical CDF\nGiven a sample $x_1, x_2, \\ldots, x_n$, the **empirical cumulative distribution function** is:\n$$\\hat{F}_n(x) = \\frac{1}{n}\\#\\{x_i \\leq x\\} = \\frac{1}{n}\\sum_{i=1}^{n} \\mathbf{1}(x_i \\leq x)$$\n\nThis assigns probability $1/n$ to each observed data point.\n:::\n\n. . .\n\n**Key properties of $\\hat{F}_n$:**\n\n- $E_{\\hat{F}}[X] = \\bar{x}$ (mean under empirical distribution = sample mean)\n- $\\text{Var}_{\\hat{F}}[X] = \\frac{1}{n}\\sum(x_i - \\bar{x})^2$ (slightly different from $s^2$)\n\n## Visualizing the Bootstrap Idea\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nlibrary(patchwork)\n\nset.seed(551)\n# Simulated \"population\"\npop <- rgamma(10000, shape = 2, rate = 0.1)\n\n# Take a sample\nn <- 30\nsamp <- sample(pop, n)\n\n# Generate bootstrap samples\nboot_means <- replicate(10000, mean(sample(samp, n, replace = TRUE)))\n\np1 <- tibble(x = pop) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"darkgreen\", alpha = 0.6) +\n  geom_vline(xintercept = mean(pop), color = \"darkgreen\", linewidth = 1.5) +\n  labs(title = \"Population (Unknown in Practice)\", \n       subtitle = bquote(mu == .(round(mean(pop), 2))),\n       x = \"Value\", y = \"Count\") +\n  theme(plot.title = element_text(size = 16))\n\np2 <- tibble(x = samp) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 15, fill = \"steelblue\", alpha = 0.7) +\n  geom_vline(xintercept = mean(samp), color = \"steelblue\", linewidth = 1.5) +\n  labs(title = \"Our Sample (What We Have)\", \n       subtitle = bquote(bar(x) == .(round(mean(samp), 2))),\n       x = \"Value\", y = \"Count\") +\n  theme(plot.title = element_text(size = 16))\n\np3 <- tibble(x = boot_means) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"coral\", alpha = 0.7) +\n  geom_vline(xintercept = mean(boot_means), color = \"coral\", linewidth = 1.5) +\n  labs(title = \"Bootstrap Distribution of Means\", \n       subtitle = paste0(\"Mean = \", round(mean(boot_means), 2), \", SE = \", round(sd(boot_means), 2)),\n       x = expression(bar(x)^\"*\"), y = \"Count\") +\n  theme(plot.title = element_text(size = 16))\n\np1 + p2 + p3\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-3-1.png){width=1344}\n:::\n:::\n\n\n## Sampling Distribution vs. Bootstrap Distribution\n\n::: columns\n::: {.column width=\"50%\"}\n**Sampling Distribution:**\n\n- Draw **many samples** from the **population**\n- Calculate statistic for each\n- Distribution of these statistics\n\n*In practice:* We can't do this (we only have one sample!)\n:::\n\n::: {.column width=\"50%\"}\n**Bootstrap Distribution:**\n\n- Draw **many resamples** from the **sample**\n- Calculate statistic for each\n- Distribution of these statistics\n\n*In practice:* We can do this computationally!\n:::\n:::\n\n. . .\n\n::: callout-note\n## Key Point\nThe bootstrap distribution **approximates** the sampling distribution, allowing us to estimate standard errors and construct confidence intervals.\n:::\n\n# Why Sampling WITH Replacement? {background-color=\"#8e44ad\"}\n\n## The Critical Question\n\nWhy must we sample **with replacement** rather than **without replacement** (subsampling)?\n\n. . .\n\n::: callout-warning\n## Without Replacement\nIf we sample **without replacement** from our sample of size $n$, we would get:\n\n- A subsample of size $m < n$: Less information, different properties\n- A sample of size $n$: **Exactly the same sample every time!**\n:::\n\n. . .\n\n**Sampling with replacement allows:**\n\n1. Observations to appear multiple times (or not at all)\n2. Variability in the bootstrap samples\n3. Approximation of the sampling process from the population\n\n## Mathematical Justification\n\n::: callout-important\n## Why Replacement Matters\nWhen sampling from a population of size $N$, we sample with replacement because:\n\n1. The population is treated as **infinite** (or very large relative to $n$)\n2. Each draw is **independent** of previous draws\n3. Each observation has the **same probability** of selection\n\nThe empirical distribution $\\hat{F}_n$ is a **discrete** distribution with $n$ equally likely values. Sampling from it **with replacement** mimics sampling from the true population.\n:::\n\n## Demonstrating: With vs. Without Replacement\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(551)\noriginal_sample <- c(3, 5, 7, 9, 11)\nn <- length(original_sample)\n\n# With replacement: many possible samples\ncat(\"With replacement (5 examples):\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nWith replacement (5 examples):\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"true\"}\nfor(i in 1:5) {\n  boot_samp <- sample(original_sample, n, replace = TRUE)\n  cat(\"  Sample\", i, \":\", boot_samp, \"-> mean =\", mean(boot_samp), \"\\n\")\n}\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  Sample 1 : 5 9 11 5 11 -> mean = 8.2 \n  Sample 2 : 7 3 3 9 3 -> mean = 5 \n  Sample 3 : 5 9 9 5 9 -> mean = 7.4 \n  Sample 4 : 7 5 11 5 11 -> mean = 7.8 \n  Sample 5 : 3 7 11 7 7 -> mean = 7 \n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"true\"}\ncat(\"\\nWithout replacement:\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nWithout replacement:\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"true\"}\ncat(\"  Always get:\", sample(original_sample, n, replace = FALSE), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  Always get: 5 3 11 7 9 \n```\n\n\n:::\n:::\n\n\n## Number of Possible Bootstrap Samples\n\n::: callout-note\n## Counting Bootstrap Samples\nFor a sample of size $n$, the number of possible bootstrap samples (sampling $n$ with replacement) is:\n$$\\binom{2n-1}{n-1} = \\binom{2n-1}{n}$$\n\nThis is the number of ways to choose $n$ items from $n$ categories with repetition allowed.\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Number of bootstrap samples for various n\ntibble(n = c(5, 10, 15, 20, 30)) |>\n  mutate(\n    num_bootstrap_samples = choose(2*n - 1, n - 1),\n    formatted = format(num_bootstrap_samples, big.mark = \",\", scientific = FALSE)\n  )\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 5 × 3\n      n num_bootstrap_samples formatted               \n  <dbl>                 <dbl> <chr>                   \n1     5               1.26e 2 \"                   126\"\n2    10               9.24e 4 \"                92,378\"\n3    15               7.76e 7 \"            77,558,760\"\n4    20               6.89e10 \"        68,923,264,410\"\n5    30               5.91e16 \"59,132,290,782,430,704\"\n```\n\n\n:::\n:::\n\n\nEven for $n = 15$, there are over **77 million** possible bootstrap samples!\n\n## The Variance Connection\n\n::: callout-important\n## Variance of Bootstrap Mean\nFor the sample mean, the **theoretical** (exhaustive) bootstrap standard error is:\n$$SE_{boot}(\\bar{X}^*) = \\frac{\\hat{\\sigma}}{\\sqrt{n}} = \\sqrt{\\frac{1}{n^2}\\sum_{i=1}^{n}(x_i - \\bar{x})^2}$$\n\nwhere $\\hat{\\sigma}^2 = \\frac{1}{n}\\sum(x_i - \\bar{x})^2$ is the population variance under $\\hat{F}_n$.\n:::\n\n. . .\n\n**Note:** This differs slightly from $s/\\sqrt{n}$ by a factor of $\\sqrt{(n-1)/n}$:\n$$SE_{boot} = \\sqrt{\\frac{n-1}{n}} \\cdot \\frac{s}{\\sqrt{n}}$$\n\nThis is why bootstrap SEs are slightly **narrower** than classical SEs.\n\n## Subsampling: An Alternative Approach\n\n::: callout-note\n## Subsampling (Without Replacement)\nAn alternative to bootstrap is **subsampling**: draw samples of size $m < n$ **without replacement**.\n\n**Properties:**\n\n- Requires choosing subsample size $m$\n- Typically $m/n \\to 0$ as $n \\to \\infty$\n- Can work when bootstrap fails (heavy tails, dependent data)\n- Generally less efficient than bootstrap\n:::\n\n. . .\n\n**When to use subsampling:**\n\n- Time series or dependent data\n- Estimating extreme quantiles\n- When bootstrap consistency fails\n\n# The Bootstrap Method {background-color=\"#9b59b6\"}\n\n## Basic Bootstrap Algorithm (Devore 8.5)\n\n::: callout-important\n## Basic Bootstrap Method\nGiven a sample $x_1, x_2, \\ldots, x_n$ and a statistic $\\hat{\\theta}$:\n\n1. **Resample:** Draw a sample of size $n$ **with replacement** from $x_1, \\ldots, x_n$\n2. **Compute:** Calculate $\\hat{\\theta}^*$ from this bootstrap sample\n3. **Repeat:** Repeat steps 1-2 many times (typically $B = 1000$ to $10000$)\n4. **Analyze:** Use the distribution of $\\hat{\\theta}^*_1, \\hat{\\theta}^*_2, \\ldots, \\hat{\\theta}^*_B$ for inference\n:::\n\n. . .\n\n**Why sample with replacement?**\n\nWithout replacement, we'd get the same sample every time!\n\n## Simple Example: Birth Weights (Chihara 5.1)\n\nConsider just 3 birth weights: 3969, 3204, 2892 grams\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsmall_sample <- c(3969, 3204, 2892)\ncat(\"Original sample:\", small_sample, \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nOriginal sample: 3969 3204 2892 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Sample mean:\", mean(small_sample))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSample mean: 3355\n```\n\n\n:::\n:::\n\n\n. . .\n\n**All possible bootstrap samples** (sampling with replacement):\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# All 3^3 = 27 bootstrap samples\nexpand_grid(x1 = small_sample, x2 = small_sample, x3 = small_sample) |>\n  mutate(\n    sample_id = row_number(),\n    mean = (x1 + x2 + x3) / 3\n  ) |>\n  head(10)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 10 × 5\n      x1    x2    x3 sample_id  mean\n   <dbl> <dbl> <dbl>     <int> <dbl>\n 1  3969  3969  3969         1  3969\n 2  3969  3969  3204         2  3714\n 3  3969  3969  2892         3  3610\n 4  3969  3204  3969         4  3714\n 5  3969  3204  3204         5  3459\n 6  3969  3204  2892         6  3355\n 7  3969  2892  3969         7  3610\n 8  3969  2892  3204         8  3355\n 9  3969  2892  2892         9  3251\n10  3204  3969  3969        10  3714\n```\n\n\n:::\n:::\n\n\n## Complete Bootstrap Distribution (Small Example)\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nall_boot_samples <- expand_grid(x1 = small_sample, x2 = small_sample, x3 = small_sample) |>\n  mutate(boot_mean = (x1 + x2 + x3) / 3)\n\nall_boot_samples |>\n  ggplot(aes(x = boot_mean)) +\n  geom_histogram(bins = 10, fill = \"steelblue\", color = \"white\", alpha = 0.7) +\n  geom_vline(xintercept = mean(small_sample), color = \"red\", linewidth = 1.2, linetype = \"dashed\") +\n  scale_x_continuous(breaks = sort(unique(all_boot_samples$boot_mean))) +\n  labs(x = expression(bar(x)^\"*\"), y = \"Frequency\",\n       title = \"Bootstrap Distribution of Mean (All 27 Samples)\",\n       subtitle = paste0(\"Original sample mean = \", round(mean(small_sample), 1))) +\n  theme(axis.text.x = element_text(angle = 45, hjust = 1))\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-8-1.png){width=672}\n:::\n:::\n\n\nBootstrap standard error: 266.2 grams\n\n## Bootstrap in R: Step by Step\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Original response time data\nresponse_times <- c(8, 12, 15, 7, 22, 45, 11, 9, 14, 18, 10, 13)\nn <- length(response_times)\nB <- 10000  # Number of bootstrap samples\n\n# Storage for bootstrap means\nboot_means <- numeric(B)\n\n# Bootstrap loop\nfor (i in 1:B) {\n  # Step 1: Resample with replacement\n  boot_sample <- sample(response_times, size = n, replace = TRUE)\n  \n  # Step 2: Calculate statistic\n  boot_means[i] <- mean(boot_sample)\n}\n\n# Results\ncat(\"Original sample mean:\", round(mean(response_times), 2), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nOriginal sample mean: 15.33 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap mean:\", round(mean(boot_means), 2), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap mean: 15.3 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap standard error:\", round(sd(boot_means), 2))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap standard error: 2.82\n```\n\n\n:::\n:::\n\n\n## Bootstrap Distribution: Drug Response Times\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\ntibble(x = boot_means) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"coral\", color = \"white\", alpha = 0.7) +\n  geom_vline(xintercept = mean(response_times), color = \"blue\", linewidth = 1.2, linetype = \"dashed\") +\n  geom_vline(xintercept = mean(boot_means), color = \"red\", linewidth = 1, linetype = \"dotted\") +\n  labs(x = expression(bar(x)^\"*\"), y = \"Count\",\n       title = \"Bootstrap Distribution of Mean Response Time\",\n       subtitle = paste0(\"B = \", B, \" resamples; SE = \", round(sd(boot_means), 2), \" days\")) +\n  annotate(\"text\", x = 12, y = 800, \n           label = paste0(\"Original mean = \", round(mean(response_times), 1)), \n           color = \"blue\", size = 5)\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-10-1.png){width=672}\n:::\n:::\n\n\n## Using Tidyverse for Bootstrap\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# More elegant approach using tidyverse\nset.seed(551)\nboot_results <- tibble(\n  replicate = 1:B\n) |>\n  mutate(\n    boot_sample = map(replicate, ~sample(response_times, n, replace = TRUE)),\n    boot_mean = map_dbl(boot_sample, mean),\n    boot_median = map_dbl(boot_sample, median),\n    boot_sd = map_dbl(boot_sample, sd)\n  )\n\nboot_results |>\n  select(-boot_sample) |>\n  summarize(\n    across(starts_with(\"boot_\"), list(mean = mean, se = sd))\n  ) |>\n  pivot_longer(everything(), names_to = \"statistic\", values_to = \"value\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 6 × 2\n  statistic        value\n  <chr>            <dbl>\n1 boot_mean_mean   15.3 \n2 boot_mean_se      2.82\n3 boot_median_mean 12.5 \n4 boot_median_se    1.79\n5 boot_sd_mean      9.04\n6 boot_sd_se        3.91\n```\n\n\n:::\n:::\n\n\n# Asymptotic Properties of Bootstrap {background-color=\"#e67e22\"}\n\n## Bootstrap Consistency\n\n::: callout-important\n## Bootstrap Consistency Theorem\nUnder regularity conditions, the bootstrap distribution consistently estimates the sampling distribution. Specifically:\n\nLet $\\hat{\\theta}_n$ be an estimator of $\\theta$ based on a sample of size $n$. If:\n$$\\sqrt{n}(\\hat{\\theta}_n - \\theta) \\xrightarrow{d} N(0, \\sigma^2)$$\n\nThen under the bootstrap:\n$$\\sqrt{n}(\\hat{\\theta}_n^* - \\hat{\\theta}_n) \\xrightarrow{d} N(0, \\sigma^2)$$\n\nwhere convergence is in probability.\n:::\n\n. . .\n\n**In plain English:** The bootstrap distribution mimics the true sampling distribution as $n \\to \\infty$.\n\n## What Bootstrap Estimates Well\n\n::: callout-tip\n## Bootstrap Accurately Estimates (Chihara 5.2.2)\n\n1. **Spread:** The spread (standard error) of the sampling distribution\n2. **Shape:** The shape (including skewness) of the sampling distribution\n3. **Bias:** The bias of the estimator\n:::\n\n. . .\n\n::: callout-warning\n## Bootstrap Does NOT Estimate\n\n**Center:** The bootstrap distribution is centered at $\\hat{\\theta}$ (the sample statistic), NOT at $\\theta$ (the true parameter).\n\nThis means bootstrap **cannot improve point estimates**!\n:::\n\n## Visualizing What Bootstrap Captures\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(551)\n# True population: Gamma(2, 0.1)\ntrue_mean <- 20\ntrue_se <- 20 / sqrt(50)\n\n# One sample and its bootstrap\nsamp <- rgamma(50, shape = 2, rate = 0.1)\nsample_mean <- mean(samp)\n\n# Bootstrap distribution\nboot_dist <- replicate(10000, mean(sample(samp, 50, replace = TRUE)))\n\n# Sampling distribution (simulated)\nsampling_dist <- replicate(10000, mean(rgamma(50, shape = 2, rate = 0.1)))\n\np1 <- tibble(x = sampling_dist) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"darkgreen\", alpha = 0.6) +\n  geom_vline(xintercept = true_mean, color = \"darkgreen\", linewidth = 1.5) +\n  labs(title = \"True Sampling Distribution\",\n       subtitle = paste0(\"Centered at μ = \", true_mean, \", SE = \", round(sd(sampling_dist), 2)),\n       x = expression(bar(X)), y = \"Count\")\n\np2 <- tibble(x = boot_dist) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"coral\", alpha = 0.6) +\n  geom_vline(xintercept = sample_mean, color = \"coral\", linewidth = 1.5) +\n  labs(title = \"Bootstrap Distribution\",\n       subtitle = paste0(\"Centered at x̄ = \", round(sample_mean, 2), \", SE = \", round(sd(boot_dist), 2)),\n       x = expression(bar(X)^\"*\"), y = \"Count\")\n\np1 + p2\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-12-1.png){width=1152}\n:::\n:::\n\n\n**Key observation:** Same spread and shape, but different centers!\n\n## Comparison Table (Chihara Table 5.2)\n\n\n::: {.cell}\n::: {.cell-output-display}\n\n\n|Quantity                   | Mean | Standard Deviation |\n|:--------------------------|:----:|:------------------:|\n|Population                 |  μ   |         σ          |\n|Sampling distribution of X̄ |  μ   |        σ/√n        |\n|Sample                     |  x̄   |         s          |\n|Bootstrap distribution     | ≈ x̄  | ≈ s·√((n-1)/n)/√n  |\n\n\n:::\n:::\n\n\n. . .\n\n**Key insights:**\n\n- Bootstrap mean ≈ sample mean (not population mean)\n- Bootstrap SE ≈ sampling distribution SE\n- Bootstrap shape ≈ sampling distribution shape\n\n## Rate of Convergence\n\n::: callout-note\n## Edgeworth Expansion Refinement\nFor the sample mean, classical confidence intervals have coverage error of order $O(n^{-1/2})$.\n\n**Bootstrap percentile intervals:** Also $O(n^{-1/2})$ — same as classical\n\n**Bootstrap t intervals:** $O(n^{-1})$ — **one order more accurate!**\n\nThis is because the bootstrap t automatically corrects for skewness.\n:::\n\n. . .\n\n**Practical implication:** Bootstrap t intervals are preferred for skewed distributions, especially with moderate sample sizes.\n\n# When Bootstrap Fails {background-color=\"#c0392b\"}\n\n## Bootstrap Failure Modes\n\nThe bootstrap is **not** a universal solution. It can fail when:\n\n1. **Very small samples** ($n < 10$)\n2. **Sample doesn't represent population**\n3. **Estimating extreme quantiles**\n4. **Discrete statistics** (e.g., median with few unique values)\n5. **Heavy-tailed distributions** (infinite variance)\n6. **Dependent data** (time series, spatial data)\n7. **Lattice distributions** with small support\n\n## Failure Mode 1: Very Small Samples\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(551)\n# Small sample from normal\nsmall_n <- 6\nsmall_samp <- rnorm(small_n, mean = 10, sd = 3)\n\n# Multiple bootstrap distributions from same sample\nboot_dists <- map(1:4, function(seed) {\n  set.seed(seed)\n  replicate(1000, mean(sample(small_samp, small_n, replace = TRUE)))\n})\n\n# True sampling distribution\ntrue_sampling <- replicate(10000, mean(rnorm(small_n, 10, 3)))\n\nplots <- map2(boot_dists, 1:4, function(bd, i) {\n  tibble(x = bd) |>\n    ggplot(aes(x = x)) +\n    geom_histogram(bins = 30, fill = \"coral\", alpha = 0.7) +\n    geom_vline(xintercept = mean(small_samp), color = \"blue\", linewidth = 1) +\n    labs(title = paste(\"Bootstrap\", i), \n         subtitle = paste0(\"SE = \", round(sd(bd), 2)),\n         x = expression(bar(x)^\"*\"), y = \"\") +\n    coord_cartesian(xlim = c(6, 14))\n})\n\nwrap_plots(plots, ncol = 4)\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-14-1.png){width=1344}\n:::\n:::\n\n\n**Problem:** Bootstrap distributions vary substantially due to small sample size.\n\n## Failure Mode 2: The Median\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(551)\n# Sample for median bootstrap\nmed_sample <- rnorm(15, mean = 0, sd = 1)\n\n# Bootstrap the median\nboot_medians <- replicate(10000, median(sample(med_sample, 15, replace = TRUE)))\n\ntibble(x = boot_medians) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 30, fill = \"purple\", alpha = 0.7) +\n  geom_vline(xintercept = median(med_sample), color = \"blue\", linewidth = 1.2) +\n  labs(x = expression(tilde(x)^\"*\"), y = \"Count\",\n       title = \"Bootstrap Distribution of Median (n = 15)\",\n       subtitle = \"Notice the discrete, irregular shape\")\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-15-1.png){width=672}\n:::\n:::\n\n\n**Problem:** Bootstrap medians only take values from the original sample!\n\n## Why Median Bootstrap Fails\n\n::: callout-warning\n## The Median Problem (Chihara 5.8.3)\nFor odd sample size $n$, the bootstrap median can only take values that are **observations in the original sample**.\n\n**Consequences:**\n\n- Bootstrap distribution is **discrete**, not continuous\n- Shape depends heavily on **gaps between observations**\n- May poorly approximate the continuous sampling distribution\n:::\n\n. . .\n\n**Remedies:**\n\n- **Smoothed bootstrap:** Add small noise to observations\n- **Larger samples:** Problem diminishes as $n$ increases\n- Use percentile intervals rather than bootstrap t\n\n## Failure Mode 3: Heavy-Tailed Distributions\n\n::: callout-important\n## Cauchy Distribution Example\nThe Cauchy distribution has **no mean** (undefined). For such distributions:\n\n- Sample mean is **not consistent**\n- Bootstrap cannot \"fix\" an inconsistent estimator\n- Bootstrap distribution doesn't converge properly\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Cauchy has no mean - bootstrap can't help\nset.seed(551)\ncauchy_sample <- rcauchy(50)\nboot_cauchy <- replicate(1000, mean(sample(cauchy_sample, 50, replace = TRUE)))\n\ncat(\"Sample mean:\", round(mean(cauchy_sample), 2), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSample mean: 0.22 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap SE:\", round(sd(boot_cauchy), 2), \" (meaningless!)\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap SE: 0.57  (meaningless!)\n```\n\n\n:::\n:::\n\n\n## Visualizing Heavy-Tail Failure\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Compare bootstrap for normal vs Cauchy\nset.seed(551)\nnormal_boot <- replicate(5000, mean(sample(rnorm(30), 30, replace = TRUE)))\ncauchy_boot <- replicate(5000, mean(sample(rcauchy(30), 30, replace = TRUE)))\n\np1 <- tibble(x = normal_boot) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"steelblue\", alpha = 0.7) +\n  labs(title = \"Bootstrap Means: Normal(0,1)\", x = expression(bar(x)^\"*\")) +\n  coord_cartesian(xlim = c(-1, 1))\n\np2 <- tibble(x = cauchy_boot) |>\n  filter(abs(x) < 50) |>  # Remove extreme values for visualization\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"coral\", alpha = 0.7) +\n  labs(title = \"Bootstrap Means: Cauchy (truncated)\", x = expression(bar(x)^\"*\"))\n\np1 + p2\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-17-1.png){width=672}\n:::\n:::\n\n\nFor Cauchy, the bootstrap distribution is erratic and doesn't converge!\n\n## Failure Mode 4: Extreme Quantiles\n\n::: callout-warning\n## Estimating Extreme Quantiles\nBootstrap has difficulty with extreme quantiles (e.g., 99th percentile) because:\n\n1. Few observations near the tail\n2. Bootstrap samples may not include extreme values\n3. Standard bootstrap underestimates tail variability\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Bootstrap for 95th percentile\nset.seed(551)\nsample_data <- rexp(50, rate = 0.1)\nboot_q95 <- replicate(5000, quantile(sample(sample_data, 50, replace = TRUE), 0.95))\n\ncat(\"Sample 95th percentile:\", round(quantile(sample_data, 0.95), 2), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSample 95th percentile: 32.73 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap SE:\", round(sd(boot_q95), 2))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap SE: 6.64\n```\n\n\n:::\n:::\n\n\n. . .\n\nFor extreme quantiles, consider **parametric bootstrap** or **extreme value theory**.\n\n## Summary: When Bootstrap Works and Fails\n\n\n::: {.cell}\n::: {.cell-output-display}\n\n\n|Works Well                |Use Caution            |May Fail                  |\n|:-------------------------|:----------------------|:-------------------------|\n|Sample mean               |Median (small n)       |Heavy tails (no variance) |\n|Trimmed mean              |Extreme quantiles      |Dependent data            |\n|Correlation               |Small samples (n < 15) |Non-representative sample |\n|Regression coefficients   |Heavily skewed data    |Maximum/minimum           |\n|Smooth functions of means |Discrete distributions |Lattice distributions     |\n\n\n:::\n:::\n\n\n# Bootstrap Standard Errors {background-color=\"#e74c3c\"}\n\n## Bootstrap Standard Error\n\n::: callout-important\n## Definition\nThe **bootstrap standard error** of a statistic $\\hat{\\theta}$ is the standard deviation of its bootstrap distribution:\n\n$$SE_{boot}(\\hat{\\theta}) = \\sqrt{\\frac{1}{B-1}\\sum_{b=1}^{B}(\\hat{\\theta}^*_b - \\bar{\\hat{\\theta}}^*)^2}$$\n\nwhere $\\bar{\\hat{\\theta}}^* = \\frac{1}{B}\\sum_{b=1}^{B}\\hat{\\theta}^*_b$\n:::\n\n. . .\n\nThis gives us an **empirical estimate** of the standard error without assuming any specific distribution!\n\n## Medical Example: Blood Pressure Reduction\n\nA pilot study measures systolic blood pressure reduction (mmHg) after 8 weeks on a new antihypertensive:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbp_reduction <- c(12, 8, 15, 22, 5, 18, 11, 14, 9, 25, 7, 16, 13, 10, 19)\nn <- length(bp_reduction)\n\ncat(\"Sample size:\", n, \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSample size: 15 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Mean reduction:\", round(mean(bp_reduction), 2), \"mmHg\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nMean reduction: 13.6 mmHg\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Sample SD:\", round(sd(bp_reduction), 2), \"mmHg\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSample SD: 5.67 mmHg\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Classical SE:\", round(sd(bp_reduction)/sqrt(n), 2), \"mmHg\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nClassical SE: 1.46 mmHg\n```\n\n\n:::\n:::\n\n\n## Bootstrap SE for Blood Pressure\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(551)\nB <- 10000\nboot_means_bp <- replicate(B, {\n  boot_sample <- sample(bp_reduction, n, replace = TRUE)\n  mean(boot_sample)\n})\n\n# Compare standard errors\nclassical_se <- sd(bp_reduction) / sqrt(n)\nboot_se <- sd(boot_means_bp)\n\ncat(\"Classical SE (s/√n):\", round(classical_se, 3), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nClassical SE (s/√n): 1.463 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap SE:\", round(boot_se, 3), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap SE: 1.41 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Ratio:\", round(boot_se / classical_se, 3))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nRatio: 0.964\n```\n\n\n:::\n:::\n\n\n. . .\n\nThe bootstrap SE is very close to the classical SE when assumptions are met!\n\n## When Bootstrap SE Shines: Trimmed Mean\n\nFor **trimmed means** (robust to outliers), we don't have simple SE formulas.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Data with potential outlier\nresponse_times <- c(8, 12, 15, 7, 22, 45, 11, 9, 14, 18, 10, 13)\n\n# Bootstrap for 10% trimmed mean\nset.seed(551)\nboot_trimmed <- replicate(10000, {\n  x <- sample(response_times, length(response_times), replace = TRUE)\n  mean(x, trim = 0.10)  # Trim 10% from each end\n})\n\ncat(\"Sample mean:\", round(mean(response_times), 2), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSample mean: 15.33 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"10% Trimmed mean:\", round(mean(response_times, trim = 0.10), 2), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n10% Trimmed mean: 13.2 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap SE of trimmed mean:\", round(sd(boot_trimmed), 2))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap SE of trimmed mean: 2.67\n```\n\n\n:::\n:::\n\n\n## Example: Database Administrator Salaries (Devore 8.18)\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Sample salaries (in thousands) - simulated based on Devore example\nset.seed(123)\nsalaries <- round(c(rlnorm(100, log(80), 0.3), rlnorm(15, log(150), 0.4))) * 1000\nn <- length(salaries)\n\ntibble(x = salaries) |>\n  ggplot(aes(x = x/1000)) +\n  geom_histogram(bins = 25, fill = \"steelblue\", alpha = 0.7) +\n  labs(x = \"Salary (thousands $)\", y = \"Count\",\n       title = \"Distribution of Database Administrator Salaries\",\n       subtitle = paste0(\"n = \", n, \", with right skew\"))\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-23-1.png){width=672}\n:::\n:::\n\n\n## Bootstrap for Trimmed Mean Salary\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 10% trimmed mean\nxbar_tr <- mean(salaries, trim = 0.10)\n\n# Bootstrap\nset.seed(551)\nB <- 10000\nboot_trimmed_sal <- replicate(B, {\n  x <- sample(salaries, n, replace = TRUE)\n  mean(x, trim = 0.10)\n})\n\ncat(\"10% Trimmed mean:\", round(xbar_tr, 0), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n10% Trimmed mean: 88430 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap SE:\", round(sd(boot_trimmed_sal), 0), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap SE: 2808 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap mean of trimmed means:\", round(mean(boot_trimmed_sal), 0))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap mean of trimmed means: 88561\n```\n\n\n:::\n:::\n\n\n## Visualizing the Bootstrap Distribution\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\ntibble(x = boot_trimmed_sal) |>\n  ggplot(aes(x = x/1000)) +\n  geom_histogram(bins = 50, fill = \"coral\", alpha = 0.7) +\n  geom_vline(xintercept = xbar_tr/1000, color = \"blue\", linewidth = 1.2) +\n  labs(x = expression(bar(x)[tr]^\"*\" ~ \"(thousands $)\"), y = \"Count\",\n       title = \"Bootstrap Distribution of 10% Trimmed Mean Salary\",\n       subtitle = paste0(\"SE = $\", round(sd(boot_trimmed_sal)/1000, 1), \"k\"))\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-25-1.png){width=672}\n:::\n:::\n\n\nThe bootstrap distribution appears approximately normal — good for t-based intervals!\n\n## Bias in Bootstrap Distributions (Chihara 5.6)\n\n::: callout-note\n## Bootstrap Bias Estimate\nThe **bootstrap estimate of bias** is:\n$$\\widehat{\\text{Bias}} = \\bar{\\hat{\\theta}}^* - \\hat{\\theta}$$\n\nwhere $\\bar{\\hat{\\theta}}^*$ is the mean of the bootstrap distribution and $\\hat{\\theta}$ is the original sample statistic.\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Check bias for trimmed mean\nboot_bias <- mean(boot_trimmed_sal) - xbar_tr\ncat(\"Bootstrap bias estimate:\", round(boot_bias, 0), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap bias estimate: 131 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bias as % of estimate:\", round(100 * boot_bias / xbar_tr, 2), \"%\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBias as % of estimate: 0.15 %\n```\n\n\n:::\n:::\n\n\n. . .\n\nSmall bias suggests the bootstrap distribution is well-centered!\n\n# Bootstrap Confidence Intervals {background-color=\"#27ae60\"}\n\n## Types of Bootstrap Confidence Intervals\n\nSeveral methods exist for constructing bootstrap CIs:\n\n1. **Bootstrap Percentile Interval** — Simple, intuitive\n2. **Bootstrap t Interval** — Better theoretical properties\n3. **BCa Interval** — Bias-corrected and accelerated (advanced)\n\n. . .\n\n::: callout-tip\n## Practical Guidance\n- **Percentile interval:** Good when bootstrap distribution is symmetric, low bias\n- **Bootstrap t interval:** Better for small samples, handles skewness\n- Both require the bootstrap distribution to be a reasonable approximation\n:::\n\n## Bootstrap Percentile Interval (Devore 8.5)\n\n::: callout-important\n## Definition\nA **95% bootstrap percentile interval** for parameter $\\theta$ has endpoints equal to the **2.5th and 97.5th percentiles** of the bootstrap distribution.\n\nMore generally, a $100(1-\\alpha)\\%$ bootstrap percentile interval uses the $\\alpha/2$ and $1-\\alpha/2$ quantiles.\n:::\n\n. . .\n\n**Intuition:** If 95% of bootstrap statistics fall between $L$ and $U$, then $(L, U)$ is a plausible range for the true parameter.\n\n## Computing Percentile Intervals in R\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Blood pressure reduction data\nset.seed(551)\nbp_reduction <- c(12, 8, 15, 22, 5, 18, 11, 14, 9, 25, 7, 16, 13, 10, 19)\n\n# Bootstrap\nboot_means_bp <- replicate(10000, mean(sample(bp_reduction, length(bp_reduction), replace = TRUE)))\n\n# 95% Percentile interval\npercentile_ci <- quantile(boot_means_bp, probs = c(0.025, 0.975))\n\ncat(\"Sample mean:\", round(mean(bp_reduction), 2), \"mmHg\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSample mean: 13.6 mmHg\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"95% Bootstrap Percentile CI:\", round(percentile_ci, 2), \"mmHg\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n95% Bootstrap Percentile CI: 10.93 16.47 mmHg\n```\n\n\n:::\n\n```{.r .cell-code}\n# Compare to t-interval\nt_ci <- t.test(bp_reduction)$conf.int\ncat(\"95% t-interval:\", round(t_ci, 2), \"mmHg\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n95% t-interval: 10.46 16.74 mmHg\n```\n\n\n:::\n:::\n\n\n## Visualizing the Percentile Interval\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\ntibble(x = boot_means_bp) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"steelblue\", alpha = 0.7) +\n  geom_vline(xintercept = percentile_ci, color = \"red\", linewidth = 1.2, linetype = \"dashed\") +\n  geom_vline(xintercept = mean(bp_reduction), color = \"black\", linewidth = 1) +\n  annotate(\"rect\", xmin = percentile_ci[1], xmax = percentile_ci[2], \n           ymin = 0, ymax = Inf, alpha = 0.2, fill = \"red\") +\n  labs(x = expression(bar(x)^\"*\"), y = \"Count\",\n       title = \"Bootstrap Distribution with 95% Percentile Interval\",\n       subtitle = paste0(\"CI: (\", round(percentile_ci[1], 1), \", \", round(percentile_ci[2], 1), \") mmHg\"))\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-28-1.png){width=672}\n:::\n:::\n\n\n## Medical Example: Median Survival Time\n\nFor **median** survival, we don't have simple formulas for CIs. Bootstrap is ideal!\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Survival times (months) for 20 cancer patients\nsurvival_times <- c(4, 8, 12, 15, 18, 6, 9, 24, 36, 11,\n                    7, 14, 21, 10, 13, 5, 17, 22, 8, 19)\n\n# Bootstrap for median\nset.seed(551)\nboot_medians <- replicate(10000, {\n  median(sample(survival_times, length(survival_times), replace = TRUE))\n})\n\n# Results\ncat(\"Sample median:\", median(survival_times), \"months\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSample median: 12.5 months\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Bootstrap SE:\", round(sd(boot_medians), 2), \"months\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap SE: 2.22 months\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"95% Percentile CI:\", quantile(boot_medians, c(0.025, 0.975)), \"months\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n95% Percentile CI: 8.5 17.5 months\n```\n\n\n:::\n:::\n\n\n## Bootstrap Distribution of Median\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nmed_ci <- quantile(boot_medians, c(0.025, 0.975))\n\ntibble(x = boot_medians) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 30, fill = \"darkgreen\", alpha = 0.7) +\n  geom_vline(xintercept = med_ci, color = \"red\", linewidth = 1.2, linetype = \"dashed\") +\n  geom_vline(xintercept = median(survival_times), color = \"black\", linewidth = 1) +\n  labs(x = expression(tilde(x)^\"*\"), y = \"Count\",\n       title = \"Bootstrap Distribution of Median Survival Time\",\n       subtitle = \"Notice: Discrete nature of the median\")\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-30-1.png){width=672}\n:::\n:::\n\n\nThe bootstrap median distribution takes on **few unique values** — this is typical!\n\n## Bootstrap t Confidence Interval (Devore 8.5)\n\n::: callout-important\n## Bootstrap t Interval\nIf the bootstrap distribution is approximately **normal** with small **bias**, use:\n$$\\hat{\\theta} \\pm t_{\\alpha/2, n-1} \\cdot SE_{boot}$$\n\nwhere $SE_{boot}$ is the bootstrap standard error.\n:::\n\n. . .\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Bootstrap t interval for trimmed mean salary\nxbar_tr <- mean(salaries, trim = 0.10)\nse_boot <- sd(boot_trimmed_sal)\nt_crit <- qt(0.975, df = length(salaries) - 1)\n\nci_lower <- xbar_tr - t_crit * se_boot\nci_upper <- xbar_tr + t_crit * se_boot\n\ncat(\"Bootstrap t 95% CI for trimmed mean salary:\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap t 95% CI for trimmed mean salary:\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"($\", round(ci_lower, 0), \", $\", round(ci_upper, 0), \")\", sep = \"\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n($82868, $93992)\n```\n\n\n:::\n:::\n\n\n## The Studentized Bootstrap t Interval (Chihara 7.5)\n\n::: callout-important\n## Bootstrap t Interval (More Sophisticated)\nFor each bootstrap sample, compute:\n$$T^* = \\frac{\\bar{X}^* - \\bar{x}}{S^*/\\sqrt{n}}$$\n\nUse quantiles $Q^*_1$ and $Q^*_2$ of the bootstrap t distribution:\n$$\\left(\\bar{x} - Q^*_2 \\cdot \\frac{s}{\\sqrt{n}}, \\; \\bar{x} - Q^*_1 \\cdot \\frac{s}{\\sqrt{n}}\\right)$$\n:::\n\n. . .\n\nThis method adapts to **skewness** in the data!\n\n## Bootstrap t Interval: R Implementation\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrap_t_interval <- function(data, B = 10000, conf = 0.95) {\n  n <- length(data)\n  xbar <- mean(data)\n  se <- sd(data) / sqrt(n)\n  \n  # Compute bootstrap t statistics\n  t_star <- replicate(B, {\n    boot_samp <- sample(data, n, replace = TRUE)\n    (mean(boot_samp) - xbar) / (sd(boot_samp) / sqrt(n))\n  })\n  \n  # Quantiles\n  alpha <- 1 - conf\n  q_lower <- quantile(t_star, alpha/2)\n  q_upper <- quantile(t_star, 1 - alpha/2)\n  \n  # CI (note the reversal!)\n  c(xbar - q_upper * se, xbar - q_lower * se)\n}\n\n# Apply to blood pressure data\nboot_t_ci <- bootstrap_t_interval(bp_reduction)\ncat(\"Bootstrap t CI:\", round(boot_t_ci, 2), \"mmHg\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap t CI: 10.73 17.17 mmHg\n```\n\n\n:::\n:::\n\n\n## Comparing All Methods\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Compare all CI methods for BP reduction data\nset.seed(551)\n\n# Classical t\nt_result <- t.test(bp_reduction)$conf.int\n\n# Bootstrap percentile\nboot_stats <- replicate(10000, mean(sample(bp_reduction, length(bp_reduction), replace = TRUE)))\npct_ci <- quantile(boot_stats, c(0.025, 0.975))\n\n# Bootstrap t\nboot_t_ci <- bootstrap_t_interval(bp_reduction)\n\ntibble(\n  Method = c(\"Classical t\", \"Bootstrap Percentile\", \"Bootstrap t\"),\n  Lower = c(t_result[1], pct_ci[1], boot_t_ci[1]),\n  Upper = c(t_result[2], pct_ci[2], boot_t_ci[2])\n) |>\n  mutate(\n    Width = Upper - Lower,\n    across(Lower:Width, ~round(.x, 2))\n  )\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 3 × 4\n  Method               Lower Upper Width\n  <chr>                <dbl> <dbl> <dbl>\n1 Classical t           10.5  16.7  6.28\n2 Bootstrap Percentile  10.9  16.5  5.53\n3 Bootstrap t           10.8  17.2  6.47\n```\n\n\n:::\n:::\n\n\n## When Percentile May Be Suboptimal: Skewed Distributions\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Highly skewed data\nset.seed(551)\nskewed_data <- rexp(15, rate = 0.5)  # Exponential with mean 2\n\nboot_skewed <- replicate(10000, mean(sample(skewed_data, 15, replace = TRUE)))\npct_ci <- quantile(boot_skewed, c(0.025, 0.975))\n\ntibble(x = boot_skewed) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"coral\", alpha = 0.7) +\n  geom_vline(xintercept = pct_ci, color = \"blue\", linewidth = 1.2, linetype = \"dashed\") +\n  geom_vline(xintercept = mean(skewed_data), color = \"black\", linewidth = 1) +\n  labs(x = expression(bar(x)^\"*\"), y = \"Count\",\n       title = \"Bootstrap Distribution from Skewed Population\",\n       subtitle = \"Percentile interval may have incorrect coverage\")\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-34-1.png){width=672}\n:::\n:::\n\n\n# Practical Considerations {background-color=\"#16a085\"}\n\n## How Many Bootstrap Samples? (Chihara 5.9)\n\n::: callout-note\n## Guidelines\n- **Standard errors:** $B = 1,000$ is often sufficient\n- **Confidence intervals:** $B = 5,000$ to $10,000$ recommended\n- **Percentile tails:** More samples needed for accurate tail quantiles\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Demonstrating convergence of bootstrap SE\nset.seed(551)\nB_values <- c(100, 500, 1000, 5000, 10000)\n\nconvergence <- map_dfr(B_values, function(B) {\n  boot_means <- replicate(B, mean(sample(bp_reduction, length(bp_reduction), replace = TRUE)))\n  tibble(B = B, SE = sd(boot_means))\n})\n\nconvergence\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 5 × 2\n      B    SE\n  <dbl> <dbl>\n1   100  1.51\n2   500  1.37\n3  1000  1.38\n4  5000  1.41\n5 10000  1.41\n```\n\n\n:::\n:::\n\n\n## Visualizing Bootstrap Convergence\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(551)\n# Run multiple bootstrap analyses with different B\nrunning_se <- tibble(\n  B = 1:10000,\n  cumulative_se = accumulate(\n    replicate(10000, mean(sample(bp_reduction, length(bp_reduction), replace = TRUE))),\n    ~c(.x, .y),\n    .init = numeric(0)\n  )[-1] |> map_dbl(sd)\n)\n\nrunning_se |>\n  ggplot(aes(x = B, y = cumulative_se)) +\n  geom_line(color = \"steelblue\") +\n  geom_hline(yintercept = tail(running_se$cumulative_se, 1), linetype = \"dashed\", color = \"red\") +\n  labs(x = \"Number of Bootstrap Samples (B)\", y = \"Bootstrap SE\",\n       title = \"Convergence of Bootstrap Standard Error\") +\n  scale_x_log10()\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-36-1.png){width=672}\n:::\n:::\n\n\n## When Does Bootstrap Work Well?\n\n::: callout-tip\n## Bootstrap Works Well When:\n1. The sample is **representative** of the population\n2. The sample size is **not too small** ($n \\geq 15$ typically)\n3. The statistic is **smooth** (means, variances, regression coefficients)\n4. The parameter is well-defined and has finite variance\n:::\n\n. . .\n\n::: callout-warning\n## Bootstrap May Struggle When:\n1. Sample is very small ($n < 10$)\n2. Data has extreme outliers not representative of population\n3. Statistic is very discrete (e.g., median with few unique values)\n4. Estimating extreme quantiles\n5. Population has infinite variance (Cauchy, Pareto with $\\alpha \\leq 2$)\n:::\n\n## Classical vs. Bootstrap: Guidelines (Chihara 7.5.1)\n\n\n::: {.cell}\n::: {.cell-output-display}\n\n\n|Situation                               |Recommendation                       |\n|:---------------------------------------|:------------------------------------|\n|Small n (≤10), normal data              |Classical t preferred                |\n|Small n (≤10), skewed data              |Bootstrap t (with caution)           |\n|Large n, normal data                    |Either works well                    |\n|Large n, skewed data                    |Bootstrap t preferred                |\n|Complex statistic (trimmed mean, ratio) |Bootstrap (no classical alternative) |\n\n\n:::\n:::\n\n\n. . .\n\n**Key insight:** The bootstrap **learns skewness from the sample**, but small samples may not accurately represent population skewness.\n\n## Medical Application: Relative Risk (Chihara 5.5)\n\nIn a cardiovascular study:\n\n- High BP group: 55 events in 3,338 men\n- Low BP group: 21 events in 2,676 men\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Relative risk\np1 <- 55/3338  # High BP group\np2 <- 21/2676  # Low BP group\nrr_hat <- p1/p2\n\ncat(\"Risk (high BP):\", round(p1 * 100, 2), \"%\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nRisk (high BP): 1.65 %\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Risk (low BP):\", round(p2 * 100, 2), \"%\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nRisk (low BP): 0.78 %\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"Relative Risk:\", round(rr_hat, 2))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nRelative Risk: 2.1\n```\n\n\n:::\n:::\n\n\n## Bootstrap for Relative Risk\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(551)\nn1 <- 3338; x1 <- 55\nn2 <- 2676; x2 <- 21\n\n# Bootstrap\nboot_rr <- replicate(10000, {\n  # Resample from each group (binary outcomes)\n  boot_p1 <- sum(sample(c(rep(1, x1), rep(0, n1 - x1)), n1, replace = TRUE)) / n1\n  boot_p2 <- sum(sample(c(rep(1, x2), rep(0, n2 - x2)), n2, replace = TRUE)) / n2\n  \n  # Handle division by zero\n  if (boot_p2 == 0) return(NA)\n  boot_p1 / boot_p2\n})\n\nboot_rr <- boot_rr[!is.na(boot_rr)]\n\ncat(\"Bootstrap SE:\", round(sd(boot_rr), 3), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBootstrap SE: 0.627 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"95% Percentile CI:\", round(quantile(boot_rr, c(0.025, 0.975)), 2))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n95% Percentile CI: 1.3 3.7\n```\n\n\n:::\n:::\n\n\n## Relative Risk: Bootstrap Distribution\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nrr_ci <- quantile(boot_rr, c(0.025, 0.975))\n\ntibble(x = boot_rr) |>\n  ggplot(aes(x = x)) +\n  geom_histogram(bins = 50, fill = \"coral\", alpha = 0.7) +\n  geom_vline(xintercept = rr_hat, color = \"blue\", linewidth = 1.2) +\n  geom_vline(xintercept = rr_ci, color = \"red\", linewidth = 1, linetype = \"dashed\") +\n  labs(x = \"Relative Risk\", y = \"Count\",\n       title = \"Bootstrap Distribution of Relative Risk\",\n       subtitle = paste0(\"RR = \", round(rr_hat, 2), \", 95% CI: (\", \n                        round(rr_ci[1], 2), \", \", round(rr_ci[2], 2), \")\"))\n```\n\n::: {.cell-output-display}\n![](09_Bootstrap_Introduction_old_files/figure-html/unnamed-chunk-40-1.png){width=672}\n:::\n:::\n\n\nHigh BP patients have 1.3 to 3.7 times the cardiovascular risk (95% CI).\n\n# Summary and Looking Ahead {background-color=\"#2c3e50\"}\n\n## Key Takeaways\n\n::: callout-important\n## The Bootstrap Principle\n1. **The sample represents the population** — resample from the sample\n2. **Sampling with replacement** mimics drawing new samples\n3. **Bootstrap distribution approximates sampling distribution**\n4. Use bootstrap to estimate **standard errors** and construct **confidence intervals**\n:::\n\n. . .\n\n**Main advantages:**\n\n- No distributional assumptions required\n- Works for **any statistic** (medians, ratios, etc.)\n- Computationally straightforward\n\n## Why Resampling WITH Replacement?\n\n::: callout-note\n## Key Points\n1. **Without replacement:** Would get the same sample every time (size $n$)\n2. **With replacement:** Creates variability that mimics sampling from population\n3. **Empirical distribution:** Assigns probability $1/n$ to each observation\n4. **Number of bootstrap samples:** $\\binom{2n-1}{n}$ possible — astronomically large!\n:::\n\n## Asymptotic Properties Summary\n\n::: callout-tip\n## Bootstrap Consistency\nUnder regularity conditions:\n\n- Bootstrap distribution **converges** to sampling distribution\n- Bootstrap SE **consistently estimates** true SE\n- Bootstrap t intervals have coverage error $O(n^{-1})$ — better than percentile!\n:::\n\n. . .\n\n**The bootstrap accurately estimates:**\n\n- Spread (standard error)\n- Shape (including skewness)\n- Bias\n\n**But NOT the center** (stays at $\\hat{\\theta}$, not $\\theta$)\n\n## When Bootstrap Fails: Summary\n\n| Failure Mode | Reason | Alternative |\n|--------------|--------|-------------|\n| Very small $n$ | Sample doesn't represent population | Classical methods |\n| Median/quantiles | Discrete bootstrap distribution | Smoothed bootstrap |\n| Heavy tails | Infinite variance | Subsampling |\n| Extreme quantiles | Few observations in tails | Parametric bootstrap |\n| Dependent data | Independence assumption violated | Block bootstrap |\n\n## Bootstrap CIs Summary\n\n| Method | Formula | When to Use |\n|--------|---------|-------------|\n| **Percentile** | $(\\hat{\\theta}^*_{\\alpha/2}, \\hat{\\theta}^*_{1-\\alpha/2})$ | Symmetric bootstrap dist., low bias |\n| **Bootstrap t** | $\\hat{\\theta} \\pm t_{\\alpha/2} \\cdot SE_{boot}$ | Normal bootstrap dist. |\n| **Bootstrap t (studentized)** | Uses bootstrap t-statistics | Skewed data, more accurate |\n\n## What the Bootstrap Can and Cannot Do (Chihara 5.2.2)\n\n::: columns\n::: {.column width=\"50%\"}\n**Can estimate:**\n\n- Standard error\n- Bias\n- Skewness\n- Confidence intervals\n:::\n\n::: {.column width=\"50%\"}\n**Cannot improve:**\n\n- Point estimates (still centered at $\\hat{\\theta}$, not $\\theta$)\n- A bad/unrepresentative sample\n:::\n:::\n\n. . .\n\n::: callout-warning\n## Important Reminder\nThe bootstrap does **not create new information**. It uses the sample you have to understand the variability of your estimate.\n:::\n\n## Looking Ahead\n\n**Next Topics:**\n\n- **Two-sample bootstrap** — comparing groups\n- **Bootstrap hypothesis testing** — computing p-values\n- **Permutation tests** — alternative resampling approach\n\n. . .\n\n**Coming Soon:**\n\n- Hypothesis testing framework (Lesson 10)\n- Type I and Type II errors\n- Power and sample size\n\n## References\n\n::: nonincremental\n-   Devore, Berk, and Carlton. *Modern Mathematical Statistics with Applications* (Springer). Section 8.5\n-   Chihara and Hesterberg. *Mathematical Statistics with Resampling and R* (Wiley). Chapters 5, 7\n-   Efron, B. and Tibshirani, R. (1993). *An Introduction to the Bootstrap*. Chapman & Hall.\n:::\n\n## Questions? {.center background-color=\"#3498db\"}\n\nThank you!\n\n\n",
    "supporting": [
      "09_Bootstrap_Introduction_old_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}