{
  "hash": "7d63a24bddfebda4d1a7408a4811c59b",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Lesson 19: Two-Sample Inference for Proportions and Variances\"\nsubtitle: \"BSTA 551: Statistical Inference\"\nauthor: \"Jessica Minnier\"\ntitle-slide-attributes:\n    data-background-color: \"#006a4e\"\ndate: \"March 4, 2026\"\nformat: \n  revealjs:\n    theme: \"../simple_NW.scss\"\n    chalkboard: true\n    scrollable: true\n    slide-number: true\n    show-slide-number: all\n    width: 1955\n    height: 1100\n    footer: Lesson 19 - Two-Sample Proportions and Variances\n    html-math-method: mathjax\n    highlight-style: atom-one\nexecute:\n  echo: true\n  warning: false\n  message: false\n---\n\n\n\n# Lesson 19: Two-Sample Proportions and Variances {background-color=\"#2c3e50\"}\n\n## Review: Lesson 18\n\n**Two independent samples for means:**\n\n- z-procedures: $\\sigma_1^2, \\sigma_2^2$ known → pivot $Z \\sim N(0,1)$\n- Welch t-test: $\\sigma_1^2, \\sigma_2^2$ unknown, unequal → $T_W \\approx t_{\\hat\\nu}$\n- Pooled t-test: $\\sigma_1^2 = \\sigma_2^2$ unknown, equal → $T_P \\sim t_{n_1+n_2-2}$\n\n. . .\n\n**Today's Goals:**\n\n1. Extend two-sample inference to **binary outcomes**: CIs and tests for $p_1 - p_2$\n2. Understand **pooled vs. unpooled** SE in the two-sample proportion setting\n3. Derive the **F-distribution** and use it for inference about $\\sigma_1^2/\\sigma_2^2$\n4. Apply the F-test for **equality of variances** and understand its limitations\n5. Apply all methods to clinical trial and epidemiologic examples\n\n## Roadmap\n\n| Parameter | Setting | Method | Reference |\n|-----------|---------|--------|-----------|\n| $p_1 - p_2$ | Large samples | z-test (pooled or unpooled SE) | DBC 9.6 |\n| $p_1 - p_2$ | Any | CI for difference in proportions | DBC 9.6 |\n| $\\sigma_1^2/\\sigma_2^2$ | Normal populations | F-test | DBC 9.6 |\n\n. . .\n\n::: callout-note\n## Motivating Examples\n1. **Clinical trial:** Does drug A reduce 30-day readmission rate compared to standard care? Compare $\\hat{p}_1$ vs $\\hat{p}_2$.\n2. **Quality control:** Is measurement variability the same in two labs? Compare $S_1^2$ vs $S_2^2$.\n:::\n\n# Two-Sample Inference for Proportions (DBC 9.6) {background-color=\"#3498db\"}\n\n## Setup: Two Independent Bernoulli Samples\n\n**Group 1:** $X_{11}, \\ldots, X_{1n_1} \\stackrel{iid}{\\sim} \\text{Bernoulli}(p_1)$\n\n**Group 2:** $X_{21}, \\ldots, X_{2n_2} \\stackrel{iid}{\\sim} \\text{Bernoulli}(p_2)$\n\nIndependent groups. Observed counts: $Y_1 = \\sum X_{1j} \\sim \\text{Bin}(n_1, p_1)$, $Y_2 = \\sum X_{2j} \\sim \\text{Bin}(n_2, p_2)$.\n\n**Parameter of interest:** $\\Delta = p_1 - p_2$\n\n**Point estimator:** $\\hat\\Delta = \\hat{p}_1 - \\hat{p}_2 = Y_1/n_1 - Y_2/n_2$\n\n. . .\n\n**Sampling distribution** (by CLT, large samples):\n\n$$\\hat{p}_1 - \\hat{p}_2 \\;\\dot{\\sim}\\; N\\!\\left(p_1 - p_2,\\; \\frac{p_1(1-p_1)}{n_1} + \\frac{p_2(1-p_2)}{n_2}\\right)$$\n\nStandard error:\n$$\\sigma_{\\hat\\Delta} = \\sqrt{\\frac{p_1(1-p_1)}{n_1} + \\frac{p_2(1-p_2)}{n_2}}$$\n\n## CI for $p_1 - p_2$\n\nPlugging in $\\hat{p}_1$ and $\\hat{p}_2$ for the unknown $p_1, p_2$ gives the **Wald CI** (DBC 9.6):\n\n$$(\\hat{p}_1 - \\hat{p}_2) \\pm z_{\\alpha/2} \\sqrt{\\frac{\\hat{p}_1(1-\\hat{p}_1)}{n_1} + \\frac{\\hat{p}_2(1-\\hat{p}_2)}{n_2}}$$\n\n. . .\n\n::: callout-note\n## Rule of Thumb for Large Sample Approximation\nThe normal approximation is adequate when:\n$$n_i \\hat{p}_i \\geq 5 \\quad \\text{and} \\quad n_i(1-\\hat{p}_i) \\geq 5 \\quad \\text{for } i = 1, 2$$\nFor rare events or small $n$, use **Fisher's exact test** or **bootstrap CI** (Lesson 21).\n:::\n\n## Hypothesis Testing for $p_1 - p_2$\n\n**Test:** $H_0: p_1 = p_2$ vs $H_a: p_1 \\neq p_2$ (or one-sided)\n\nUnder $H_0$, both groups share a common $p$. Estimate it by the **pooled proportion**:\n$$\\hat{p} = \\frac{Y_1 + Y_2}{n_1 + n_2}$$\n\n**Pooled z-statistic** (use for hypothesis tests):\n$$Z = \\frac{\\hat{p}_1 - \\hat{p}_2}{\\sqrt{\\hat{p}(1-\\hat{p})\\left(\\frac{1}{n_1} + \\frac{1}{n_2}\\right)}}$$\n\n. . .\n\n::: callout-important\n## Pooled vs. Unpooled SE\n- **Test:** Use **pooled** $\\hat{p}$ because $H_0$ states $p_1 = p_2$ — we exploit this constraint.\n- **CI:** Use **unpooled** $\\hat{p}_1, \\hat{p}_2$ because we make no assumption about $p_1 = p_2$.\n:::\n\n## Medical Example: Adverse Event Rates\n\n**Clinical Trial:** A drug trial compares 30-day serious adverse event (SAE) rates between treatment arm ($n_1 = 150$, $y_1 = 12$ SAEs) and control ($n_2 = 148$, $y_2 = 24$ SAEs).\n\nTest $H_0: p_1 = p_2$ vs. $H_a: p_1 \\neq p_2$ at $\\alpha = 0.05$ and construct a 95% CI.\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"false\"}\nn1 <- 150; y1 <- 12\nn2 <- 148; y2 <- 24\n\np1_hat <- y1/n1; p2_hat <- y2/n2\np_pool <- (y1 + y2)/(n1 + n2)\n\n# Test statistic (pooled SE)\nse_pool <- sqrt(p_pool*(1-p_pool)*(1/n1 + 1/n2))\nz_stat  <- (p1_hat - p2_hat) / se_pool\np_val   <- 2 * pnorm(abs(z_stat), lower.tail = FALSE)\n\n# 95% CI (unpooled SE)\nse_ci <- sqrt(p1_hat*(1-p1_hat)/n1 + p2_hat*(1-p2_hat)/n2)\nci    <- (p1_hat - p2_hat) + c(-1,1) * qnorm(0.975) * se_ci\n\ncat(\"Treatment SAE rate:\", round(p1_hat, 4), \n    \" | Control SAE rate:\", round(p2_hat, 4), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nTreatment SAE rate: 0.08  | Control SAE rate: 0.1622 \n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"Difference:\", round(p1_hat - p2_hat, 4), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nDifference: -0.0822 \n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"Pooled SE:\", round(se_pool, 4),\n    \" | Z-stat:\", round(z_stat, 3),\n    \" | p-value:\", round(p_val, 4), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nPooled SE: 0.0378  | Z-stat: -2.176  | p-value: 0.0296 \n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"95% CI for p1-p2: (\", round(ci[1], 4), \",\", round(ci[2], 4), \")\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n95% CI for p1-p2: ( -0.1557 , -0.0086 )\n```\n\n\n:::\n:::\n\n\n## Compare with prop.test()\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"false\"}\n# R built-in (uses chi-squared with continuity correction by default)\nprop.test(c(y1, y2), c(n1, n2), correct = FALSE) |> tidy()\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 1 × 9\n  estimate1 estimate2 statistic p.value parameter conf.low conf.high method     \n      <dbl>     <dbl>     <dbl>   <dbl>     <dbl>    <dbl>     <dbl> <chr>      \n1      0.08     0.162      4.73  0.0296         1   -0.156  -0.00860 2-sample t…\n# ℹ 1 more variable: alternative <chr>\n```\n\n\n:::\n:::\n\n\n. . .\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Visualize the SAE rates with CI\ntribble(\n  ~Group, ~rate, ~lo, ~hi,\n  \"Treatment\", p1_hat, p1_hat - qnorm(0.975)*sqrt(p1_hat*(1-p1_hat)/n1),\n                        p1_hat + qnorm(0.975)*sqrt(p1_hat*(1-p1_hat)/n1),\n  \"Control\",   p2_hat, p2_hat - qnorm(0.975)*sqrt(p2_hat*(1-p2_hat)/n2),\n                        p2_hat + qnorm(0.975)*sqrt(p2_hat*(1-p2_hat)/n2)\n) |>\n  ggplot(aes(x = Group, y = rate, color = Group)) +\n  geom_point(size = 5) +\n  geom_errorbar(aes(ymin = lo, ymax = hi), width = 0.1, linewidth = 1.2) +\n  scale_color_manual(values = c(\"Treatment\" = \"#006a4e\", \"Control\" = \"#e74c3c\")) +\n  scale_y_continuous(labels = scales::percent, limits = c(0, 0.25)) +\n  labs(title = \"30-Day Serious Adverse Event Rate by Treatment Arm\",\n       subtitle = \"Points: estimated rates; bars: 95% Wald CIs\",\n       x = NULL, y = \"SAE Rate\", color = NULL) +\n  theme(legend.position = \"none\")\n```\n\n::: {.cell-output-display}\n![](19_TwoSample_ProportionsVariances_files/figure-revealjs/unnamed-chunk-3-1.png){width=960}\n:::\n:::\n\n\n## Relative Risk and Odds Ratio (Brief)\n\nThe difference $p_1 - p_2$ is one measure of treatment effect. In medical research, two ratios are commonly reported:\n\n$$\\text{Relative Risk (RR)} = \\frac{p_1}{p_2}, \\qquad \\text{Odds Ratio (OR)} = \\frac{p_1/(1-p_1)}{p_2/(1-p_2)}$$\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"false\"}\n# Estimated RR and OR\nRR <- p1_hat / p2_hat\nOR <- (p1_hat/(1-p1_hat)) / (p2_hat/(1-p2_hat))\n\n# Log-scale CIs (normal approximation)\nlog_RR_ci <- log(RR) + c(-1,1) * qnorm(0.975) * \n             sqrt(1/y1 - 1/n1 + 1/y2 - 1/n2)  # approx SE(log RR)\nlog_OR_ci <- log(OR) + c(-1,1) * qnorm(0.975) *\n             sqrt(1/y1 + 1/(n1-y1) + 1/y2 + 1/(n2-y2))\n\ncat(sprintf(\"RR = %.3f (95%% CI: %.3f, %.3f)\\n\",\n            RR, exp(log_RR_ci[1]), exp(log_RR_ci[2])))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nRR = 0.493 (95% CI: 0.256, 0.949)\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(sprintf(\"OR = %.3f (95%% CI: %.3f, %.3f)\\n\",\n            OR, exp(log_OR_ci[1]), exp(log_OR_ci[2])))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nOR = 0.449 (95% CI: 0.216, 0.936)\n```\n\n\n:::\n:::\n\n\n. . .\n\n**Interpretation:** The treatment arm had approximately half the SAE rate of control (RR ≈ 0.49), though the CI crosses values that would be borderline.\n\n# Inferences About Two Variances (DBC 9.6) {background-color=\"#e74c3c\"}\n\n## The F-Distribution\n\nWhen comparing variability between two groups, we use the **F-distribution**.\n\n**Setup:** If $S_1^2$ and $S_2^2$ are sample variances from independent normal samples:\n\n$$F = \\frac{S_1^2/\\sigma_1^2}{S_2^2/\\sigma_2^2} = \\frac{S_1^2}{S_2^2} \\cdot \\frac{\\sigma_2^2}{\\sigma_1^2} \\sim F_{n_1-1,\\,n_2-1}$$\n\nwhere $F_{m,n}$ denotes the **F-distribution** with $m$ numerator and $n$ denominator degrees of freedom.\n\n. . .\n\n**Key Properties of $F_{m,n}$:**\n- Support: $(0, \\infty)$; right-skewed\n- $E[F] = n/(n-2)$ for $n > 2$\n- If $F \\sim F_{m,n}$, then $1/F \\sim F_{n,m}$ (**reciprocal relationship**)\n- $F_{m,\\infty} \\sim \\chi^2_m / m$\n- Critical values: `qf(p, df1, df2)` in R\n\n## F-Distribution Shape\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nx_f <- seq(0.01, 6, length.out = 500)\ndf_shapes <- tibble(\n  x      = rep(x_f, 5),\n  df1    = rep(c(1, 5, 10, 20, 50), each = 500),\n  df2    = rep(c(1, 5, 10, 20, 50), each = 500),\n  dens   = map2_dbl(x, df1, ~ df(.x, df1 = .y, df2 = .y)),\n  label  = factor(rep(paste0(\"F(\", c(1,5,10,20,50), \",\", c(1,5,10,20,50), \")\"),\n                      each = 500))\n)\n\nggplot(df_shapes, aes(x = x, y = dens, color = label)) +\n  geom_line(linewidth = 1.3) +\n  scale_color_brewer(palette = \"Dark2\") +\n  coord_cartesian(ylim = c(0, 1.2)) +\n  labs(\n    title    = expression(\"F-Distribution for Various Degrees of Freedom\"),\n    subtitle = \"As both df increase, F approaches 1 and distribution concentrates\",\n    x        = expression(italic(f)),\n    y        = \"Density\",\n    color    = \"Distribution\"\n  ) +\n  theme(legend.position = \"right\")\n```\n\n::: {.cell-output-display}\n![](19_TwoSample_ProportionsVariances_files/figure-revealjs/unnamed-chunk-5-1.png){width=1248}\n:::\n:::\n\n\n## F-Test for $H_0: \\sigma_1^2 = \\sigma_2^2$\n\n**Under $H_0: \\sigma_1^2 = \\sigma_2^2$:** The pivot simplifies to $F = S_1^2/S_2^2 \\sim F_{n_1-1,\\,n_2-1}$\n\n**Test statistic:** $f = s_1^2/s_2^2$\n\n| Alternative | Rejection Region | p-value |\n|-------------|-----------------|---------|\n| $\\sigma_1^2 > \\sigma_2^2$ | $F > F_{\\alpha,\\,n_1-1,\\,n_2-1}$ | $P(F \\geq f)$ |\n| $\\sigma_1^2 < \\sigma_2^2$ | $F < F_{1-\\alpha,\\,n_1-1,\\,n_2-1}$ | $P(F \\leq f)$ |\n| $\\sigma_1^2 \\neq \\sigma_2^2$ | $F < F_{1-\\alpha/2}$ or $F > F_{\\alpha/2}$ | $2\\min(P(F \\leq f), P(F \\geq f))$ |\n\n. . .\n\n**CI for $\\sigma_1^2/\\sigma_2^2$:**\n\n$$\\left(\\frac{s_1^2/s_2^2}{F_{\\alpha/2,\\,n_1-1,\\,n_2-1}},\\;\\; \\frac{s_1^2/s_2^2}{F_{1-\\alpha/2,\\,n_1-1,\\,n_2-1}}\\right)$$\n\nNote: $F_{1-\\alpha/2,\\,m,n} = 1/F_{\\alpha/2,\\,n,m}$ (use the reciprocal relationship in tables).\n\n## Medical Example: Comparing Lab Variability\n\n**Example (DBC 9.6 style):** Two clinical laboratories each measure serum potassium (mEq/L) on the same set of patient samples. Concern: Does one lab have higher measurement variability?\n\n- Lab 1: $n_1 = 16$ samples, $s_1 = 0.38$ mEq/L\n- Lab 2: $n_2 = 21$ samples, $s_2 = 0.22$ mEq/L\n\nTest $H_0: \\sigma_1^2 = \\sigma_2^2$ vs. $H_a: \\sigma_1^2 > \\sigma_2^2$ at $\\alpha = 0.05$.\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"false\"}\nn1 <- 16; s1 <- 0.38\nn2 <- 21; s2 <- 0.22\nalpha <- 0.05\n\n# F statistic and p-value (one-sided: sigma1 > sigma2)\nf_stat <- s1^2 / s2^2\ndf1    <- n1 - 1\ndf2    <- n2 - 1\np_val  <- pf(f_stat, df1 = df1, df2 = df2, lower.tail = FALSE)\n\n# 95% CI for sigma1^2/sigma2^2\nf_lo <- qf(alpha/2,   df1 = df1, df2 = df2)\nf_hi <- qf(1-alpha/2, df1 = df1, df2 = df2)\nci   <- c(f_stat/f_hi, f_stat/f_lo)\n\ncat(\"F statistic:\", round(f_stat, 3), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nF statistic: 2.983 \n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"df: (\", df1, \",\", df2, \")\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\ndf: ( 15 , 20 )\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"One-sided p-value:\", round(p_val, 4), \"\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nOne-sided p-value: 0.012 \n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"95% CI for sigma1^2/sigma2^2: (\",\n    round(ci[1], 3), \",\", round(ci[2], 3), \")\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n95% CI for sigma1^2/sigma2^2: ( 1.159 , 8.222 )\n```\n\n\n:::\n:::\n\n\n. . .\n\n**Conclusion:** $F = 2.983$, $p = 0.012$. Strong evidence that Lab 1 has greater measurement variability than Lab 2.\n\n## Using var.test() in R\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"false\"}\nset.seed(551)\n# Simulate individual observations matching the summaries above\nlab1_vals <- rnorm(n1, mean = 4.2, sd = s1)\nlab2_vals <- rnorm(n2, mean = 4.2, sd = s2)\n\n# F-test\nvar.test(lab1_vals, lab2_vals, alternative = \"greater\") |> tidy()\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 1 × 9\n  estimate num.df den.df statistic p.value conf.low conf.high method alternative\n     <dbl>  <int>  <int>     <dbl>   <dbl>    <dbl>     <dbl> <chr>  <chr>      \n1     3.04     15     20      3.04  0.0109     1.38       Inf F tes… greater    \n```\n\n\n:::\n:::\n\n\n::: callout-warning\n## Sensitivity to Normality\nThe F-test for equality of variances is **extremely sensitive to non-normality** — much more so than the t-test for means. For non-normal data, prefer:\n\n- **Levene's test** (`car::leveneTest()`) — uses absolute deviations from medians\n- **Bartlett's test** — equivalent to F-test, also sensitive to non-normality\n\nIn medical research, the F-test for variances is most useful as a **descriptive tool** or when normality is well-established (e.g., lab measurements).\n:::\n\n## Sensitivity of F-test to Non-normality\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(551)\nB <- 3000\n\nsim_ftest <- function(dist_fn, n1 = 20, n2 = 20) {\n  map_dbl(1:B, function(i) {\n    x1 <- dist_fn(n1)\n    x2 <- dist_fn(n2)\n    var.test(x1, x2)$p.value\n  })\n}\n\n# Under H0 (same distribution): should reject ~5% of the time\np_norm   <- sim_ftest(function(n) rnorm(n))\np_t3     <- sim_ftest(function(n) rt(n, df = 3))\np_exp    <- sim_ftest(function(n) rexp(n))\np_chisq5 <- sim_ftest(function(n) rchisq(n, df = 5))\n\ntibble(\n  p_value = c(p_norm, p_t3, p_exp, p_chisq5),\n  Distribution = rep(c(\"Normal\", \"t(3) — heavy tails\",\n                        \"Exponential — skewed\", \"χ²(5) — skewed\"),\n                     each = B)\n) |>\n  group_by(Distribution) |>\n  summarise(RejectionRate = mean(p_value < 0.05)) |>\n  ggplot(aes(x = reorder(Distribution, RejectionRate),\n             y = RejectionRate, fill = Distribution)) +\n  geom_col(width = 0.55) +\n  geom_hline(yintercept = 0.05, linetype = \"dashed\", linewidth = 1.1) +\n  scale_fill_brewer(palette = \"Set2\") +\n  scale_y_continuous(labels = scales::percent, limits = c(0, 0.3),\n                     expand = expansion(mult = c(0, 0.05))) +\n  labs(title = \"F-Test Type I Error Under H₀ (σ₁=σ₂) for Non-Normal Distributions\",\n       subtitle = \"F-test is severely inflated for heavy-tailed and skewed data. Nominal level = 5%.\",\n       x = NULL, y = \"Rejection Rate (α = 0.05)\", fill = NULL) +\n  coord_flip() +\n  theme(legend.position = \"none\",\n        axis.text.y = element_text(size = 13))\n```\n\n::: {.cell-output-display}\n![](19_TwoSample_ProportionsVariances_files/figure-revealjs/unnamed-chunk-8-1.png){width=1248}\n:::\n:::\n\n\n# Putting It Together: A Clinical Trial Analysis {background-color=\"#9b59b6\"}\n\n## Complete Two-Sample Analysis Example\n\n**Scenario:** A randomized trial tests a new anticoagulant vs. standard of care in patients with atrial fibrillation. Outcomes measured at 6 months:\n\n- **Continuous:** INR (International Normalized Ratio) — measure of clotting time\n- **Binary:** Rate of major bleeding events\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(42)\nn_treat <- 80; n_ctrl <- 78\n\n# Simulate data\ninr_treat   <- rnorm(n_treat, mean = 2.5, sd = 0.6)\ninr_ctrl    <- rnorm(n_ctrl,  mean = 2.1, sd = 0.9)\nbleed_treat <- rbinom(n_treat, 1, 0.06)\nbleed_ctrl  <- rbinom(n_ctrl,  1, 0.13)\n\n# Analyses\nwelch_res <- t.test(inr_treat, inr_ctrl) |> tidy()\nftest_res <- var.test(inr_treat, inr_ctrl) |> tidy()\nprop_res  <- prop.test(c(sum(bleed_treat), sum(bleed_ctrl)),\n                        c(n_treat, n_ctrl), correct = FALSE) |> tidy()\n\n# Plot\np1 <- ggplot(tibble(INR = c(inr_treat, inr_ctrl),\n                    Group = rep(c(\"Treatment\",\"Control\"), c(n_treat, n_ctrl))),\n             aes(x = Group, y = INR, fill = Group)) +\n  geom_violin(alpha = 0.5, trim = FALSE) +\n  geom_boxplot(width = 0.15, fill = \"white\", outlier.shape = 1) +\n  scale_fill_manual(values = c(\"Treatment\"=\"#006a4e\", \"Control\"=\"#e74c3c\")) +\n  labs(title = \"INR at 6 Months\", x = NULL, y = \"INR\", fill = NULL) +\n  theme(legend.position = \"none\")\n\nbleed_summ <- tibble(\n  Group = c(\"Treatment\", \"Control\"),\n  rate  = c(mean(bleed_treat), mean(bleed_ctrl)),\n  n     = c(n_treat, n_ctrl)\n) |>\n  mutate(se = sqrt(rate*(1-rate)/n),\n         lo = rate - qnorm(0.975)*se,\n         hi = rate + qnorm(0.975)*se)\n\np2 <- ggplot(bleed_summ, aes(x = Group, y = rate, color = Group)) +\n  geom_point(size = 6) +\n  geom_errorbar(aes(ymin = lo, ymax = hi), width = 0.08, linewidth = 1.2) +\n  scale_color_manual(values = c(\"Treatment\"=\"#006a4e\", \"Control\"=\"#e74c3c\")) +\n  scale_y_continuous(labels = scales::percent, limits = c(-0.01, 0.25)) +\n  labs(title = \"Major Bleeding Rate\", x = NULL, y = \"Rate\", color = NULL) +\n  theme(legend.position = \"none\")\n\np1 + p2 +\n  plot_annotation(title = \"Anticoagulant Trial: Two-Sample Analyses\",\n                  subtitle = \"Left: continuous outcome (INR); Right: binary outcome (bleeding rate)\")\n```\n\n::: {.cell-output-display}\n![](19_TwoSample_ProportionsVariances_files/figure-revealjs/unnamed-chunk-9-1.png){width=1440}\n:::\n:::\n\n\n## Reading the Results Together\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"=== INR: Welch t-test ===\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n=== INR: Welch t-test ===\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(sprintf(\"Treatment mean: %.2f | Control mean: %.2f\\n\",\n            mean(inr_treat), mean(inr_ctrl)))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nTreatment mean: 2.51 | Control mean: 1.99\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(sprintf(\"Difference: %.2f (95%% CI: %.2f, %.2f)\\n\",\n            welch_res$estimate, welch_res$conf.low, welch_res$conf.high))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nDifference: 0.52 (95% CI: 0.28, 0.75)\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(sprintf(\"p-value: %.4f\\n\\n\", welch_res$p.value))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\np-value: 0.0000\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"=== INR: F-test for equal variances ===\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n=== INR: F-test for equal variances ===\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(sprintf(\"S1=%.2f, S2=%.2f | F=%.3f, p=%.4f\\n\",\n            sd(inr_treat), sd(inr_ctrl),\n            ftest_res$statistic, ftest_res$p.value))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nS1=0.64, S2=0.82 | F=0.612, p=0.0312\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(\"\\n=== Bleeding Rate: Two-sample z-test ===\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\n=== Bleeding Rate: Two-sample z-test ===\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(sprintf(\"Treatment: %.1f%% | Control: %.1f%%\\n\",\n            100*mean(bleed_treat), 100*mean(bleed_ctrl)))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nTreatment: 7.5% | Control: 12.8%\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(sprintf(\"Difference: %.1f%% (95%% CI: %.1f%%, %.1f%%)\\n\",\n            100*prop_res$estimate1 - 100*prop_res$estimate2,\n            100*prop_res$conf.low, 100*prop_res$conf.high))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nDifference: -5.3% (95% CI: -14.7%, 4.1%)\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"false\"}\ncat(sprintf(\"p-value: %.4f\\n\", prop_res$p.value))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\np-value: 0.2677\n```\n\n\n:::\n:::\n\n\n# Key Takeaways {background-color=\"#2c3e50\"}\n\n## Key Takeaways\n\n::: callout-important\n## Today's Main Points (DBC 9.6)\n\n1. **Two-sample proportions:** $\\hat{p}_1 - \\hat{p}_2$ is approximately normal (CLT). Use **pooled SE** for the test (under $H_0: p_1 = p_2$) and **unpooled SE** for the CI\n\n2. **Large-sample condition:** Need $n_i \\hat{p}_i \\geq 5$ and $n_i(1-\\hat{p}_i) \\geq 5$; otherwise use Fisher's exact test or bootstrap\n\n3. **F-distribution:** Ratio of two independent scaled chi-squared variables; $S_1^2/S_2^2 \\sim F_{n_1-1,\\,n_2-1}$ under $H_0: \\sigma_1^2 = \\sigma_2^2$ and normality\n\n4. **F-test for equal variances:** Exact under normality but **very sensitive to non-normality** — use Levene's test in practice for non-normal data\n\n5. **CI for $\\sigma_1^2/\\sigma_2^2$:** Asymmetric interval based on F critical values; check whether 1 is inside (evidence for equal variances)\n\n6. **Clinical reporting:** Always report point estimates + CIs; choose measure of effect (RD, RR, OR) that is most interpretable in context\n:::\n\n## Looking Ahead\n\n**Next class: Analysis of Paired Data (DBC 10.3)**\n\n- When to use a paired design (pre/post, crossover trials, matched cases)\n- Reduction to one-sample problem: $D_i = X_{1i} - X_{2i}$\n- Paired t-test and CI\n- Nonparametric alternatives: Wilcoxon signed-rank test (DBC Chapter 14)\n\n. . .\n\n**Lesson 21:** Bootstrap and permutation tests for two samples\n\n## Homework 7 (continued) {.smaller}\n\n**Problem 4** (DBC 9.6): A vaccine trial reports 12 infections in 400 vaccinated participants and 28 infections in 395 placebo participants.\n(a) Test $H_0: p_1 = p_2$ vs. $H_a: p_1 < p_2$ at $\\alpha = 0.01$ using the pooled z-test. Compute the p-value.\n(b) Construct a 99% CI for $p_1 - p_2$ using the unpooled SE.\n(c) Compute and interpret the relative risk and odds ratio.\n(d) Verify with `prop.test()` in R.\n\n**Problem 5** (DBC 9.6): Two groups of patients have serum albumin measured (g/dL): Group A ($n_1 = 25$, $s_1 = 0.45$) and Group B ($n_2 = 30$, $s_2 = 0.72$).\n(a) Construct a 90% CI for $\\sigma_A^2/\\sigma_B^2$.\n(b) Test $H_0: \\sigma_A^2 = \\sigma_B^2$ vs. $H_a: \\sigma_A^2 \\neq \\sigma_B^2$ at $\\alpha = 0.10$.\n(c) What assumption is critical for the F-test? How would you check it?\n\n## References\n\n::: nonincremental\n- Devore, Berk, and Carlton. *Modern Mathematical Statistics with Applications* (Springer). Section 9.6.\n- Chihara, L. M. and Hesterberg, T. C. *Mathematical Statistics with Resampling and R* (Wiley). Chapter 3.\n:::\n\n## Questions? {.center background-color=\"#3498db\"}\n\nThank you!\n",
    "supporting": [
      "19_TwoSample_ProportionsVariances_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {
      "include-after-body": [
        "\n<script>\n  // htmlwidgets need to know to resize themselves when slides are shown/hidden.\n  // Fire the \"slideenter\" event (handled by htmlwidgets.js) when the current\n  // slide changes (different for each slide format).\n  (function () {\n    // dispatch for htmlwidgets\n    function fireSlideEnter() {\n      const event = window.document.createEvent(\"Event\");\n      event.initEvent(\"slideenter\", true, true);\n      window.document.dispatchEvent(event);\n    }\n\n    function fireSlideChanged(previousSlide, currentSlide) {\n      fireSlideEnter();\n\n      // dispatch for shiny\n      if (window.jQuery) {\n        if (previousSlide) {\n          window.jQuery(previousSlide).trigger(\"hidden\");\n        }\n        if (currentSlide) {\n          window.jQuery(currentSlide).trigger(\"shown\");\n        }\n      }\n    }\n\n    // hookup for slidy\n    if (window.w3c_slidy) {\n      window.w3c_slidy.add_observer(function (slide_num) {\n        // slide_num starts at position 1\n        fireSlideChanged(null, w3c_slidy.slides[slide_num - 1]);\n      });\n    }\n\n  })();\n</script>\n\n"
      ]
    },
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}